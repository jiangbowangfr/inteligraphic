1.现在打开就是project loaded ？不对
2.点击new，需要1.可以选择新建一个project，输入project name，选择路径2.可以选择新建一个design，输入design name，选择路径
  创建project时，自动创建一个floorplan 的文件夹用于存放floorplan文件(和其他design一样，点击+号，创建floorplan page，但是创建page之前需要弹出一个对话框用于配置floorplan是Hierarchical还是Tiling结构，创建的floorplan是ssn bscan jtag还是bisr，不用创建Page这个文件夹)，同时自动创建一个ip_config文件用于存放ip_config文件
  现在new project弹窗是
      // ====== 加载 NewProject 包 ======
    function ensureNewProjectLoaded(cb) {
        // 注意：这里的文件名要和你包产物一致
        ensureCss(DIST + '/dft-newproject-menu.iife.css');
        ensureScript(DIST + '/dft-newproject-menu.iife.js', cb);
    }

    // “New Project” 入口
    window.newProjectmy = function (editorUi) {
        function openOnce() {
            if (!window.DFTNewProject?.open) {
                console.error('DFTNewProject 仍未就绪');
                alert('New Project 模块未就绪: 请检查 dft-newproject-menu.iife.js 是否存在于构建目录');
                return;
            }
    这中方式好，还是直接嵌入在drawio中好，你评估一下
3. 在project explorer中 点击+号还在是直接创建一个top design，不太对，应该是怎么样的，得改一下
4. 现在的创建page rename 创建top desion的弹窗太丑了，怎么统一？和目前的flow navigator ，prject explorer的ui风格统一一下？
5. 现在每个Block design包含以下内容，也就是把page改为Arch，把env改为Flow，增加ip_config：
    1. Arch
        1. Ssn 
        2. Bscan
        3. Bisr
        4. Jtag
    2. Flow
        1. Mbist Bscan
        2. logic test
        3. s8syn and lec
        4. scan
        5. atpg
        6. sim
    3. IP_Config 
  在floorplan中选择创建floorplan时，弹窗让用户选择floorplan的类型，是Hierarchical还是Tiling结构，同时选择floorplan包含的内容是Ssn Bscan Jtag 还是Bisr，然后用户创建floorplan后也就是创建了不同的block，然后创建数据流，数据流在不同block之间的流入流出位置，然后点击生成interface就生成对应的interface接口，然后点见生成module page（改为生成desgin）就生成对应的design，包含arch和flow，如果如ssn就在ssn下生成对应的designname_ssn page
   点击flow下面的流程，比如mbist bscan，就打开对应的流程界面，流程界面先留空，后续再开发流程编辑功能
6. project有一个总的ip_config文件夹，每个design也有自己的ip_config文件，这个里面也是可以新增page的用于ip config。

context panel 中的ip library panel中需要支持导入本地的ip库文件，导入后可以在ip library panel中展示，并且可以拖拽到画布上使用。ip库文件的格式是verilog 语言，提取module名称和端口信息，端口中的input放在左侧，output放在右侧。，并在ip library panel中展示模块列表，



1. 现在打开就是project loaded ？ 不对
2.点击new，需要1.可以选择新建一个project，输入project name，选择路径或者可以选择新建一个design，输入design name，选择路径（选择路径，不是输入路径）
3.创建project时，自动创建一个floorplan 的文件夹用于存放floorplan文件(和其他design一样，点击+号，创建floorplan page,三点按钮可以增加其他功能，，同时自动创建一个ip_config文件用于存放ip_config文件

